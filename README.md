# Frontlines

--------------------------------------------------

To Do List:

- C: Create map/background
- J: Create Game ambiance
- A: Create MVP game

--------------------------------------------------

General Notes:

- Always commit before major changes
- Keep commit messages short and clear
- Use one commit per task where possible
- Use GitHub as the master backup
- To undo press: Ctrl + Z
- Have fun :)
  
--------------------------------------------------

Workflow:

1. Open terminal inside:

A:\Game Dev\Frontlines\Unity Project

2. Run:

git add .
git commit -m "Describe changes"
git push

--------------------------------------------------

Useful Git commands:

Check changed files:

git status

View commit history:

git log --oneline

View changed file names:

git diff --name-only
